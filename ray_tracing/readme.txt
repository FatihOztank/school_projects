Ray tracing project i did for Computer Graphics course which i audited.
In order for this code to work, a ppm reader is reguired. Also,
glm libary should be in your machine and included from project settings.
IO.h and PixelBuffer.h were already given by the instructor.